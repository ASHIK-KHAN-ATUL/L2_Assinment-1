Generics in TypeScript let us write reusable functions and components that work with different types while still keeping type safety.

Instead of creating separate functions for each type, we can use one generic function.

A generic uses a type parameter like <T>, which is decided when the function is used. This makes the function flexible but still safe.